/**
 * Version service — fetch proposal section version history.
 *
 * The backend stores versions inside the ProposalVersion table.
 * There is no dedicated versions list endpoint yet; versions are included
 * implicitly in the proposal response via the update flow.
 *
 * This service is a placeholder for when a dedicated endpoint is added.
 * The VersionHistory component currently renders versions passed via props.
 */

// No-op export — reserved for future backend version history endpoint.
export {}
